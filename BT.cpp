#include <iostream>

using namespace std;

template<typename T>
class Tree {
private:
    Tree*Right = nullptr
    Tree*Left = nullptr
    int data;
public:
    Beginning (T data) {
        this->data=data
        this->Right = nullptr
        this->Left = nullptr
    };

    Add (T data) {
        if (data > //smth) 
        {
            //smth->Right = data
        };
        else {
            //smth->Left = data
        }
    }

};